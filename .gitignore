# Python (backend)
backend/.venv/
**/__pycache__/
*.pyc
backend/.env

# Node (frontend)
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/*.tsbuildinfo

# Editors / OS
.DS_Store
.claude/settings.local.json

# Helm — dependency archives are regenerated by `helm dependency build`; Chart.lock stays committed
charts/*/charts/*.tgz
