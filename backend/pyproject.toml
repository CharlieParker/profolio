[project]
name = "profolio-backend"
version = "0.1.0"
description = "Profolio backend — accounts and holdings API"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.2",
    "pydantic-settings>=2.6",
]

[tool.uv]
package = false
