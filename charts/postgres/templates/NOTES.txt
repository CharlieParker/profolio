Postgres is up as a StatefulSet in {{ .Release.Namespace }}.

Check it:
  kubectl -n {{ .Release.Namespace }} rollout status sts/{{ include "postgres.fullname" . }}
  kubectl -n {{ .Release.Namespace }} get pvc

Reachable in-cluster at {{ include "postgres.fullname" . }}:{{ .Values.service.port }}
from the same namespace, or at
{{ include "postgres.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }} from another.
